# Tasks

## linear-design-system

- [x] OpenSpec 아티팩트 생성 (proposal, tasks, status)
- [x] `globals.css` — CSS 변수 Linear 팔레트로 교체, Inter Variable import, font-feature-settings
- [x] `tailwind.config.ts` — borderRadius 스케일, 신규 색상 토큰 추가
- [x] `layout.tsx` — 변경 없음 (Inter는 globals.css @import로 처리)
- [x] `Card.tsx` — var(--card-bg) / var(--card-border) CSS 변수 + 8px radius
- [x] `Button.tsx` — Primary/ghost/secondary 스타일, 6px radius, font-inter weight 510
- [x] `Badge.tsx` — subtle badge 스타일, 2px radius
- [x] `Sidebar.tsx` — Inter 폰트, Linear 활성 상태, 반투명 보더
- [x] `Header.tsx` — Linear 보더, 드롭다운 8px radius
- [x] 빌드 확인 — 신규 에러 없음 (기존 pre-existing ESLint 에러 제외)
